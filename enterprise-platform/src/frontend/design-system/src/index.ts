// @cachesol/design-system — Public API
// Package này hiện chưa có component cụ thể; sẽ được populate khi design system tokens
// đã được thống nhất trong /design-system/ ở root (markdown docs).

export const VERSION = '1.0.0';
